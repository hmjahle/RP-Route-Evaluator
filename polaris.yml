version: "1"
project:
  name: Optimization-Technologies/RP-Route-Evaluator
  revision:
    name: revision_name
    date: revision_date
  properties:
    GROUP: group_id_here
    PSC_ID: a6b5a488-1235-4c31-9e28-fbdaf63e1aa0
capture:
  fileSystem:
    ears:
      extensions: [ear]
      files:
      - directory: ${project.projectDir}
    java:
      files:
      - directory: ${project.projectDir}
    javascript:
      files:
      - directory: ${project.projectDir}
      - excludeRegex: node_modules|bower_components|vendor
    php:
      files:
      - directory: ${project.projectDir}
    python:
      files:
      - directory: ${project.projectDir}
    ruby:
      files:
      - directory: ${project.projectDir}
    wars:
      extensions: [war]
      files:
      - directory: ${project.projectDir}
analyze:
  mode: central
install:
  coverity:
    version: default
serverUrl: https://visma.polaris.synopsys.com
